# buildpilot-generate

Supabase Edge Function for BuildPilot AI.

Required Edge Function secret:
- OPENAI_API_KEY

Optional:
- OPENAI_MODEL

Function name:
- buildpilot-generate

Do not add Supabase service-role keys or OpenAI keys to GitHub.
