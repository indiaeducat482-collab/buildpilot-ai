# BuildPilot AI — STEP 4 package

This package uses the updated STEP 4 `app.js` with Website Only / Complete System project-type selection.

Existing static assets, config, styling, Supabase function, and migration files are preserved from the previous BuildPilot package.

Before testing generation, STEP 5 must update the Supabase Edge Function to accept and store the new project type fields.
